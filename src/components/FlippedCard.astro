---
const CardItems = [
  {
    colors: ["bg-[#16B78D]", "bg-[#FEF8F8]", "bg-[#FEF8F8]"],
    number: 1,
    title: "Collecter efficacement les données pertinentes.",
  },
  {
    colors: ["bg-[#70B3C1]", "bg-[#FEF8F8]", "bg-[#FEF8F8]"],
    number: 2,
    title: "Organiser et structurer vos informations pour une clarté optimale.",
  },
  {
    colors: ["bg-[#8961FD]", "bg-[#FEF8F8]", "bg-stone-50"],
    number: 3,
    title: "Organiser et structurer vos informations pour une clarté optimale.",
  },
];
---

<div class="flex flex-col">
  <section class="flex flex-col space-y-10 mt-32">
    <p
      class="text-4xl text-center text-white mx-40 p-6 bg-gradient-linear-2 rounded-2xl uppercase"
    >
    Maximisez votre croissance avec WeeMake
    </p>
    <p class="text-center text-black text-4xl">
      L'art de l'analyse de données
    </p>
    <p class="text-center"><span style="text-black text-xl">Imaginez une </span><span class="font-bold">meilleure prise de décision</span><span> au sein de votre </span><span class="font-bold">équipe</span><span>, alimentée par une </span><span class="font-bold">compréhension approfondie</span><span> de vos données.<br/>Avec nos </span><span class="font-bold">interfaces de saisie intuitives</span><span>, nous </span><span class="font-bold">simplifions</span><span > la </span><span class="font-bold">visualisation</span><span> de vos </span><span class="font-bold">projets</span><span>, vous permettant de :</span></p>
  </section>
  <div
    class="flex flex-col lg:flex-row items-center justify-around mt-20"
  >
    {
      CardItems.map((CardItem) => (
        <div
          class={`transform rotate-[45deg] w-60 h-60 ${CardItem.colors[0]} flex items-center justify-center rounded-2xl shadow-2xl`}
        >
          <div
            class={`transform -rotate-[30deg] w-44 h-44 ${CardItem.colors[1]} flex items-center justify-center rounded-2xl bg-opacity-70 shadow-2xl`}
          >
            <div
              class={`transform -rotate-[15deg] w-40 h-40 ${CardItem.colors[2]} flex items-center justify-center rounded-2xl  shadow-inner-2xl`}
            >
              <div class="text-center p-4">
                <p class="font-bold">{CardItem.number}</p>
                <p>{CardItem.title}</p>
              </div>
            </div>
          </div>
        </div>
      ))
    }
  </div>
</div>
